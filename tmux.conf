#┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
#┃ General                                                                     ┃
#┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
set-option -g mouse on
set-option -g history-limit 1000000
set-option -ga terminal-overrides ",*:Tc"
set-option -g default-terminal "screen-256color"

unbind C-b
set -g prefix M-v
bind M-v send-prefix

set-option -s set-clipboard on

# Renumber windows when a window is closed.
set -g renumber-windows on

# Start windows and panes at 1, not 0
set -g base-index      1
set -g pane-base-index 1

set -g focus-events on

set-window-option -g mode-keys vi

# Stop escape from sending Alt+
set -s escape-time 0

set-option -g allow-rename off

#┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
#┃ Mappings                                                                    ┃
#┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
# Smart pane switching with awareness of vim splits
# See: https://github.com/christoomey/vim-tmux-navigator
# is_vim="echo '#{pane_current_command}' | \
#     grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|n?vim?x?)(diff)?$'"
# is_vim="ps -o state= -o comm= -t '#{pane_tty}' | \
#     grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|n?vim?x?)(diff)?$'"
is_vim="echo '#{pane_current_command}' | \
    grep -iqE '(\\S+\\/)?g?(view|n?vim?x?)(diff)?$'"

bind-key -n C-h if-shell "$is_vim" "send-keys C-h" "select-pane -L"
bind-key -n C-j if-shell "$is_vim" "send-keys C-j" "select-pane -D"
bind-key -n C-k if-shell "$is_vim" "send-keys C-k" "select-pane -U"
bind-key -n C-l if-shell "$is_vim" "send-keys C-l" "select-pane -R"
bind-key -T copy-mode-vi C-h select-pane -L
bind-key -T copy-mode-vi C-j select-pane -D
bind-key -T copy-mode-vi C-k select-pane -U
bind-key -T copy-mode-vi C-l select-pane -R

bind-key -n              M-u copy-mode
bind-key -T copy-mode-vi M-u send-keys -X halfpage-up
bind-key -T copy-mode-vi M-d send-keys -X halfpage-down

# Use Alt+s to choose session from a list
bind-key -n     M-s choose-tree

bind-key -n M-h select-window -t :-
bind-key -n M-l select-window -t :+
bind-key -n M-\ splitw -h -c '#{pane_current_path}'
bind-key -n M-- splitw    -c '#{pane_current_path}'
bind-key -n M-N new-window

bind-key -n M-z resize-pane -Z

bind-key -n M-H resize-pane -L 5
bind-key -n M-L resize-pane -R 5
bind-key -n M-K resize-pane -U 5
bind-key -n M-J resize-pane -D 5

# Same as default but removes confirmation
bind-key    x   kill-pane
bind-key    M-x kill-pane
bind-key -n M-X kill-pane

# Use prefix + Ctrl+l/k to send sequence to shell
bind-key    M-l send-keys 'C-l' # Clear screen
bind-key    M-k send-keys 'C-k' # Delete right of cursor

bind-key -n M-R command-prompt -I "#W" "rename-window -- '%%'"
# Remap v to begin-selection
#
# unbind-key -T copy-mode-vi space

bind-key -T copy-mode-vi y send -X copy-selection-and-cancel

# bind-key -T copy-mode-vi y send -X copy-pipe "xclip -selection clipboard -i" \; send -X clear-selection
bind-key -T copy-mode-vi y send -X copy-pipe "pbcopy-remote" \; send -X clear-selection

#┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
#┃ Nesting                                                                     ┃
#┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
# bind -T root M-F11 \
#     set status-left-style           bg=colour19,fg=colour8 \;\
#     set status-right-style          bg=colour19,fg=colour8 \;\
#     set window-status-style         bg=colour19,fg=colour8 \;\
#     set window-status-current-style bg=colour18,fg=colour8,bold \;\
#     set status-style                bg=colour19,fg=colour18

# bind -T root M-F12 \
#     set -u status-left-style \;\
#     set -u status-right-style \;\
#     set -u status-style \;\
#     set -u window-status-style \;\
#     set -u window-status-current-style

# bind -T root M-k  \
#     set prefix None \;\
#     set key-table off \;\
#     set status-left-style           bg=colour19,fg=colour8 \;\
#     set status-right-style          bg=colour19,fg=colour8 \;\
#     set window-status-style         bg=colour19,fg=colour8 \;\
#     set window-status-current-style bg=colour18,fg=colour8,bold \;\
#     set status-style                bg=colour19,fg=colour18 \;\
#     send-keys M-F12 \;\
#     if -F '#{pane_in_mode}' 'send-keys -X cancel' \;\
#     refresh-client -S

# bind -T off M-k send-keys 'Escape'
# bind -T off M-j \
#     set -u prefix \;\
#     set -u status-left-style \;\
#     set -u status-right-style \;\
#     set -u status-style \;\
#     set -u window-status-style \;\
#     set -u window-status-current-style \;\
#     set -u key-table \;\
#     send-keys M-F11 \;\
#     refresh-client -S

#┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
#┃ Theme                                                                       ┃
#┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
set -g status-justify               centre
set -g status-style                 fg=colour8
set -g status-left                  ' #{session_name}'
set -g status-right                 '%a %e %b  %H:%M '
set -g window-status-format         '   #{window_name}   '
set -g window-status-current-format '   #{window_name}   '
set -g window-status-current-style  fg=colour7,bold
set -g window-status-separator      ''
set -g pane-border-style            fg=colour8
set -g message-style                bg=colour18,fg=colour7
set -g pane-active-border-style     fg=colour8

set-window-option -g visual-bell off
set-window-option -g bell-action other

#┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
#┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
