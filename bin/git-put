#! /usr/bin/env bash
set -e

to_branch=${1-master}

orig_branch=$(git rev-parse --abbrev-ref HEAD)
git branch -f "$to_branch"
git checkout "$to_branch"
remoteBranch=$(git config branch."$orig_branch".merge)
remoteBranch=${remoteBranch##refs/heads/}
git track ${2-$remoteBranch}
